class Puzzle extends Phaser.Scene
    {
        preload ()
        {
           
        }

        create ()
        {
           
        }
    }

    const config = {
        type: Phaser.AUTO,
        width: 800,
        height: 600,
        scene: Puzzle,
    };

    const game = new Phaser.Game(config);